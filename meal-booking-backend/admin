import 

class User():
    def user_login(self):
        email="user@gmail.com"
        User="password"
        if password=valid:
    "allow access"
    else:deny

   def user_signup(self):
       input{
           email="email"
           name="name"
           password:"password"
           confirm"password retype>
           if expression:
               pass
       }



